#include<iostream>
using namespace std;
int main()
{
    int i, arr[30], n, val;
    cout << "Enter the size  of an array:";
    cin >> n;
    cout << "Enter the elements of the array:";
    for (i = 0; i<n;i++)
    {
        cin >> arr[i];
    }
    // Enter the value you have to find
    // CODE HERE
    cout << "Enter the value you have to find:"<<endl;
    cin >> val;

    // Execute an iterative loop up to size
    // CODE HERE


    // use conditional loop for searching an element of the array
    // CODE HERE


    return 0;

}